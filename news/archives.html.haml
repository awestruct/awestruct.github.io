---
title: News archives
layout: default
---

- for post in site.posts
  .post
    %h1.title
      %a{:href=>post.url}= post.title
    .byline
      .author
        = post.author
      .date
        #{post.date.strftime( '%d %B %Y' )}
